// This will change the comic strip to a different one:
$("img").attr("src", "https://i.imgur.com/D9HkrX4.gif");

// Using the .attr method, change the input box into a color picker:
$("input").attr("type", "color");
